{
  "name": "thinkshout/ts_build_scripts",
  "version": "0.0.1",
  "require": {
    "composer/installers": "~1.0"
  },
    "extra": {
        "installer-paths": {
            "scripts": ["vendor/package"]
        }
    }
}
